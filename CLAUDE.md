# 프로젝트 개요

대학가 원룸촌 거주자를 대상으로 한 **생활 잡일 대행 구독 서비스**의 웹앱이다.
전구 교체, 못 박기, 가구 조립, 컴퓨터 설치 같은 작업을 예약할 수 있는 서비스를 제공한다.

## 배포 형태

- 앱스토어에 올리는 네이티브 앱이 아니라, **웹 브라우저에서 바로 접속 가능한 웹앱(PWA)**으로 만든다.
- 네이티브 전용 API나 앱스토어 심사를 전제로 한 설계는 하지 않는다.

## 서비스 메뉴 / 가격 데이터

- 현재 단계에서는 서비스 메뉴와 가격이 **아직 확정되지 않았다**. 더미(가짜) 데이터를 사용한다.
- 메뉴/가격 값은 컴포넌트나 로직 코드에 직접 하드코딩하지 않는다.
- 별도의 데이터 파일(예: `data/services.json` 또는 이에 준하는 설정 파일)로 분리해서 관리하고,
  코드에서는 이 파일을 참조하는 방식으로 작성한다. 이렇게 해야 나중에 실제 메뉴/가격이 정해졌을 때
  이 파일만 바꿔서 반영할 수 있다.

## 협업 방식

- 사용자는 코딩을 배우는 중이다. 규모가 있는 작업(새 기능 추가, 구조 변경 등)을 진행하기 전에는
  먼저 계획을 설명하고 승인을 받은 뒤에 진행한다.
- 사소하고 명확한 수정(오타 수정, 단순 스타일 조정 등)까지 매번 계획을 설명할 필요는 없다.
