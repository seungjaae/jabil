# 변경 이력

골목집사 웹앱의 버전별 변경 내용입니다.
각 버전은 git 태그와 연결되어 있어, GitHub의 **Tags** 탭에서 그 시점의 코드 전체를 볼 수 있습니다.

버전 번호는 `v0.메이저.수정` 형태로, 정식 오픈(v1.0) 전까지는 `v0.x` 를 씁니다.

---

## [미출시] 다음에 할 일

사업계획서 12번 로드맵 기준으로 남은 것들입니다.

- [ ] `data/site-data.js` 의 `contact.kakaoOpenChat` 에 오픈채팅 링크 넣기 — **이걸 채워야 문의를 받을 수 있습니다**
- [ ] `data/site-data.js` 의 `survey.url` 에 구글 폼 링크 넣기 (문항은 `docs/설문문항.md`)
- [ ] 3구역 조사 후 `brand.area` 확정
- [ ] 실제 소요 시간 데이터가 모이면 `serviceTiers` 의 임시 가격 교체
- [ ] 시공 전후 사진 30장이 모이면 포트폴리오 섹션 추가
- [ ] 후기 기능을 넣을 때 `innerHTML` 문자열 결합을 DOM 생성 방식으로 교체 (XSS 대비)

---

## [v0.2] — 2026-08-28

브로슈어에서 **실제로 쓰는 도구**로 넘어간 버전입니다.
사이트가 고객을 한 명도 받을 수 없던 문제를 고치고, 사업계획서 1~2개월차 숙제를 도와주는 화면을 추가했습니다.

### 추가

- **작업 일지** (`log.html`) — 작업별 예상/실제/이동 시간을 폰에서 기록합니다.
  작업 종류 목록은 `data/site-data.js` 의 🟢🟡 메뉴에서 자동으로 만들어집니다.
  작업별 평균 표가 자동 계산되고 (예상보다 오래 걸린 작업은 빨간색), CSV로 내려받을 수 있습니다.
  기록은 브라우저 localStorage 에만 저장되므로 주기적인 CSV 백업이 필요합니다.
  → 사업계획서 11-7 (작업 일지), 6-3 (원가 계산)
- **건물주 전용 페이지** (`owner.html`) — "5분짜리 일"이 아니라 **민원 해방 + 공실 방지**로 논리를 다시 짰습니다.
  → 사업계획서 7번 (진짜 승부처)
- **수요 조사 배너** — 구글 폼 링크를 넣으면 나타납니다. 링크가 비어 있으면 렌더링되지 않습니다.
- **설문 문항 초안** (`docs/설문문항.md`) — 8문항, 배포처와 목표 포함.
- **모바일 하단 고정 문의 버튼** 과 **햄버거 메뉴**.

### 변경

- 모든 문의 버튼(상단 CTA, 히어로, 하단 바)이 `contact.kakaoOpenChat` **한 곳**을 바라봅니다.
  링크가 비어 있으면 예전처럼 "준비 중"으로 표시됩니다.
- 구독 섹션을 페이지 뒤쪽으로 옮기고 내비게이션에서도 맨 끝으로 뺐습니다.
  → 사업계획서 6-1 "초반 4~6개월은 건당 결제로만 운영"과 순서를 맞춘 것입니다.
- 반응형 브레이크포인트에 820px 을 추가해 태블릿 구간을 메웠습니다.

### 고침

- 모바일에서 내비게이션 링크가 아무 대체 수단 없이 사라져, 폰에서는 섹션 이동이 불가능하던 문제.

### 내부

- 여러 페이지가 함께 쓰는 코드를 `js/common.js` 로 분리했습니다.
  `js/render.js` 는 메인 페이지 전용만 남았습니다.

---

## [v0.1] — 2026-08-24

첫 웹앱. 서비스 소개 랜딩 페이지입니다.

### 추가

- 원 페이지 랜딩 (`index.html`) — 히어로, 문제 제기, 서비스 메뉴, 구독 요금제, 신뢰 요소, 이용 방법, 연락처.
- **신호등 3단계 서비스 메뉴** — 🟢 바로 가능 / 🟡 2인 필요 / 🔴 하지 않는 작업.
  → 사업계획서 3번. "하지 않는 일을 먼저 공개하는 것"이 신뢰를 얻는 방식이라는 판단.
- **PWA 구성** (`manifest.json`, `sw.js`) — 홈 화면에 설치할 수 있고 오프라인에서도 화면이 뜹니다.
- **내용과 코드의 분리** — 모든 문구와 가격이 `data/site-data.js` 한 파일에 모여 있습니다.
  실제 메뉴와 가격이 정해지면 이 파일만 고치면 됩니다.

### 알려진 제약

- 서비스 메뉴의 가격은 전부 **임시(더미) 값**입니다. 실제 소요 시간 데이터를 모은 뒤 확정합니다.
- 연락처가 채워지지 않아 이 버전만으로는 문의를 받을 수 없습니다. (v0.2 에서 해결)

---

## [최초] — 2026-08-24

- 사업 계획서 작성 (`docs/사업계획서.md`).
